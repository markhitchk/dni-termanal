// Canonical DNI Node runtime environment loader.
// Keep the legacy loader as the implementation until all external VPS callers
// have migrated to this organized entrypoint.
import '../../runtime-env.mjs';
